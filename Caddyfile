{
	# Kontakt fuer Let's Encrypt: bekommt Warnung, wenn eine
	# Zertifikatserneuerung scheitert.
	email info@verinaris.de
}

demo.verinaris.de {
	# Streamlit braucht WebSockets -- Caddy leitet die automatisch durch.
	reverse_proxy frontend:8501

	encode gzip

	log {
		output stdout
		format console
	}
}
