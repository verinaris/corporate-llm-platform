# Corporate LLM Platform — Python Dependencies
# Installieren: pip install -r requirements.txt

# Web Framework (Backend)
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# LLM SDKs
anthropic==0.39.0

# Datenbank
sqlmodel==0.0.22

# Config / Validation
pydantic==2.9.2
pydantic-settings==2.6.0
python-dotenv==1.0.1
email-validator==2.2.0

# Auth (Phase 2a)
bcrypt==4.2.0
pyjwt==2.10.0

# HTTP Client
httpx==0.27.2

# Frontend (Phase 2b)
streamlit==1.40.0

# Transitive Deps explizit pinnen (Segfault-Fix 15.07.2026):
# streamlit 1.40 fordert nur pyarrow>=7.0.0 -- ohne Obergrenze.
# pip zog daher pyarrow 25.0.0 -> SIGSEGV in pyarrow/pandas_compat.py
# convert_column beim Rendern der Audit-Log-Tabelle (admin_page.py:119).
# 18.1.0 = die zu streamlit 1.40 zeitgleiche Reihe (Nov 2024).
pyarrow==18.1.0

# RAG (Phase 3a) — Vektor-DB + PDF-Reader
chromadb==0.5.20
pypdf==5.1.0

# Businessplan-Modul (Phase 5+) — Export Word/Excel/PDF
python-docx==1.1.2
openpyxl==3.1.5
reportlab==4.2.5
pandas==2.2.3

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
